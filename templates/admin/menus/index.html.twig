{% extends 'admin/menus/menus.html.twig' %}

{% block breadcrumb %}
    <li class="active">{{ 'label.menus'|trans }}</li>
{% endblock %}

{% block inner %}

    <table class="table table-striped table-middle-aligned active-datatable" data-defaultsortby="3">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">{{ 'label.slug'|trans }}</th>
            <th scope="col">{{ 'label.name'|trans }}</th>
            <th scope="col" class="text-center nosort"></th>
        </tr>
        </thead>
        <tbody>
        {% for item in list %}
            <tr>
                <td>{{ item.id }}</td>
                <td>{{ item.slug }}</td>
                <td>{{ item.name }}</td>

                <td class="text-right">
                    <div class="item-actions">

                        <a title="{{ 'action.edit'|trans }}" href="{{ path('admin_menus_edit', {id: item.id}) }}" class="btn btn-circle bg-pink waves-effect waves-circle waves-float">
                            <i class="material-icons">edit</i>
                        </a>
                        <form action="{{ url('admin_menus_delete', { id: item.id }) }}" method="post" class="delete-form">
                            <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
                            <button type="button" title="{{ 'action.delete'|trans }}" class="btn btn-circle bg-grey waves-effect waves-circle waves-float"><i class="material-icons">delete_forever</i></button>
                        </form>

                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="10" align="center">{{ 'text.noresults'|trans }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}