{% extends 'front/base.html.twig' %}

{% block body_class %}{{ parent() }} login{% endblock %}

{%  block meta_robots %}{% if page.indexed == false %}noindex,follow{% else %}{{ parent() }}{% endif %}{% endblock %}

{% block body %}

    <div class="wrapper">
        <div class="row">
            <div class="col-md-4 text-left">
                <a href="{{ path('front_homepage_index') }}"><img id="logo" src="{{ asset('build/images/logo-color.svg') }}" alt="Pix.City"></a>
            </div>
            <div class="col-md-8 text-right loginCancel">
                <a href="{{ path('front_login') }}">Annuler</a>
            </div>
        </div>


        <div class="col-md-4 left box">

            <h1>Mot de passe oublié</h1>
            <p>Entrez l’email que vous avez utilisé pour créer votre compte Pix.City.</p>
            <p>Vous allez ensuite recevoir un email pour réinitialiser votre mot de passe.</p>

        </div>
        <div class="col-md-8 right box">

            {% if not success %}
                <form action="{{ path('front_forgot_password') }}" id="forgot-password" method="POST">
                    <div class="form-group">
                        <label for="email">Email</label>
                        <div class="input-group">
                            <div class="input-group-addon">
                                <i class="fa fa-at"></i>
                            </div>
                            <input class="form-control" type="email" name="email" placeholder="{{ 'label.email'|trans }}" required autofocus>
                        </div>
                    </div>

                    {% if error %}
                        <div class="form-error">{{ error }}</div>
                    {% endif %}

                    <button class="btn transFx green btn-submit fa fa-envelope" type="submit"><span>Envoyer</span></button>
                </form>
            {% else %}
                <div class="text-center">
                    Une email a été envoyé sur votre boite email.
                    <br>
                    <br><a href="{{ path('front_login') }}" class="btn green">Retour à la connexion</a>
                </div>
            {% endif %}

        </div>

    </div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}