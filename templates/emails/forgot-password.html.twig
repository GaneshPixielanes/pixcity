{% extends 'emails/_base.html.twig' %}

{% block body %}
    <p>Bonjour,</p>
    <p>Pour réinitialiser votre mot de passe, il vous suffit de cliquer sur le lien suivant :</p>

    <table width="100%">
        <tr>
            <td align="center">
                <a style="background-color: #30b996; color: #FFF; padding: 10px 20px; display: inline-block; text-decoration: none; border-radius: 4px;" href="{{ absolute_url(path('front_change_password', {token: token})) }}">Je change mon mot de passe</a>
            </td>
        </tr>
    </table>

{% endblock %}